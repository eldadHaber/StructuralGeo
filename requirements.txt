# Core GeoGen
numpy
scipy
matplotlib
pyvista[all]
pyvistaqt
ipywidgets
ipykernel
trame
trame-vuetify
trame-vtk
tqdm
PyDTMC

# PyTorch (required for geogen.dataset and geogen.gis.dataset)
# For CUDA builds, see https://pytorch.org/get-started/locally/
torch

# geogen.gis -- Microsoft Planetary Computer + raster ingestion
pystac-client>=0.7
planetary-computer>=1.0
rioxarray>=0.15
rasterio>=1.3
xarray>=2023.1
pyproj>=3.5
shapely>=2.0
affine>=2.4

# Test runner (optional, for running the test suite)
pytest
